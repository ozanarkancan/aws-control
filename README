### aws-control

This repo contains a python script (for now) to control aws instancess via awscli api.

See the menu.

```python
python3 aws_control.py --help
```

## Dependencies

* awscli
* Pre configuration with `aws configure`
